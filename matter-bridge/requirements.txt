# Ambientika Matter Bridge - Python dependencies
# Install with: pip install -r requirements.txt

# MQTT client
paho-mqtt>=1.6.1

# Async HTTP + WebSocket (for Matter server communication)
aiohttp>=3.9.0

# python-matter-server SDK (Home Assistant Matter server)
# Provides the Matter controller that manages commissioning
# See: https://github.com/home-assistant-libs/python-matter-server
python-matter-server>=5.0.0

# Optional: enhanced logging
colorlog>=6.7.0
